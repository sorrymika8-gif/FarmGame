class_name SortingLayerConfig
extends RefCounted

## 渲染排序层级定义（Godot 使用 z_index 替代 SortingLayer）

const BACKGROUND: int = -100
const MAP_OBJECTS: int = 0
const CHARACTERS: int = 100
const WORLD_UI: int = 200
